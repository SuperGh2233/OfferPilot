<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## OfferPilot development workflow

Before changing implementation code, read `PLAN.md` and work only inside its current roadmap priority. After every code change, update `PLAN.md` in the same turn with checklist progress, validation results, the next task, and the change log. A roadmap item is complete only after its acceptance criteria and all four quality commands pass. The project requires Node.js `>=22.13.0`; verify `node --version` before quality commands and use the installed portable Node 24 runtime when the system PATH still resolves to Node 20.

## Current product baseline

- Phase 8 baseline acceptance was completed on 2026-09-14. The 2026-09-18 AI-scoring release passed anonymous Smoke; its authenticated training flow was listed for retest. User reports first-round checks completed on 2026-09-20, but no independent test artifacts, migration status or deployment result are available through this file connector. Do not assume any unverified production state.
- Supabase migrations, repeatable seed, exact catalog counts, two-user RLS isolation, Auth redirects, and production persistence have been verified. Server-side Supabase clients already use bounded retries for transient `408/429/502/503/504` and network failures; do not add duplicate route-level retry loops.
- Algorithm training already includes the Hot 100 static statement and Java starter snapshot, completed-problem import, in-page Java drafts, cancel/complete flows, deterministic mastery/review scheduling, and AI code analysis persisted on the corresponding Attempt.
- Knowledge training contains 904 questions with a 120-question six-week core. Recall persists deterministic coverage, AI semantic analysis, and effective coverage on the same Attempt. Since 2026-09-17 effective coverage is max(deterministic, AI semantic) and drives TypeScript mastery; retrying AI after submission never changes the saved mastery. Comparison across historical Attempts and focused next-review hints are still pending.
- Dashboard catch-up status derives missed training days and cumulative new-learning gaps from the plan calendar even when a past day has no generated task rows; keep these separate from exact-timestamp overdue reviews and already-generated leftover tasks.
- AI calls already have bounded timeouts, structured-output validation, server-only secrets, and client-side duplicate-click guards. Priority 6 should preserve these controls and add only the missing per-user quota, input-size policy, log redaction, and observable fallback behavior.
- Latest independently documented quality gate is the 2026-09-18 baseline: Node 24 lint/typecheck/test/build, 34 test files, 328 tests and 239 generated pages. User reports the first-round tests finished, but current connector cannot execute shell commands or verify results. Confirm migration `202609200001_daily_task_idempotency.sql` was applied before deploying dependent code.
- User-requested plan pause/resume (2026-09-21 fix, implementation unverified): `lib/profile/pause.ts` stores [start,end) training-day ranges; Settings toggles via authenticated `PATCH /api/training/profile` and SQL `set_plan_paused` or local SQLite/browser Demo. A pause must stop only new work for paused dates and cycle/streak progression: never auto-create/backfill a paused date. Debt from earlier active dates remains actionable: materialize missing historical backfill, keep previously generated pending tasks, and keep reviews already overdue before the pause visible/completable. Completing debt during a pause updates Attempt/Mastery/task status but must not count the paused completion date as an active training day or fake the original date's check-in. Reviews becoming due after the pause boundary remain deferred until resume. Apply migration `202609200002_plan_pause.sql` in staging and test RLS/real pause/resume before production deployment.
- Knowledge answer audit (2026-09-20, code written but unverified): preserve original full/core JSON and CSV; apply `data/knowledge/answer_review_patches.json` by stable question UUID through shared `lib/knowledge/content-cleaning.mjs` in BOTH runtime `lib/knowledge/catalog.ts` and `scripts/seed.mjs`. The initial 33 directly reviewed questions have been expanded by subsequent JVM/Java/concurrency edits, including the previous 41 JMM/locks/AQS/CAS revisions and a further 90 UUID-specific reviews of concurrency source orders 101–157 and collections source orders 1–37 in the latest direct-review turn; obtain the exact current number from `npm run knowledge:audit` rather than using obsolete counts. Two originally `container_only` empty questions now have answers and available status via UUID patches. Four high-impact questions additionally have fact-specific spoken recall aliases. Every question without a UUID review remains unverified; do not label the entire 904-item catalog clean. Only confirmed marketing afterwords and mechanical `：。` punctuation are auto-cleaned; never rewrite technical facts or key_points by broad regex. `data/knowledge/ANSWER_REVIEW_LOG.md` records evidence and unresolved candidates. Run `npm run knowledge:audit:report`, `seed:check`, full quality gates, then staged Seed after a backup and authenticated Recall/history regression. The 904-question audit is not finished and production catalog is unchanged. The current connection has no shell: the new review patches and tests were written but have not run through a JSON parser, Node/Vitest, staged Seed or deployment. Preserve the review progress and keep working through the remaining collections, MySQL, Redis and Spring answers rather than treating the scan report as acceptance.
- Second-round write-path optimization (2026-09-20, not yet verified): `lib/supabase/training-mutation.ts` merges persisted algorithm/knowledge mutations into the client snapshot; algorithm start/cancel/complete/AI-analysis and knowledge Learn/Recall route handlers return `{ mutation }` without a full snapshot reload. `use-cloud-training.ts` prevents an older refresh from overwriting a mutation. Import/profile still return full snapshots, and initial snapshot still contains history. Do not repeat already-implemented work; run tests and real-user regression before broadening to history pagination.

## Next optimization roadmap

Do not add new product modules until the existing daily training loop is reliable. Execute these priorities in order and reflect each task in `PLAN.md` before implementation:

1. **Completed — production acceptance.** The stable-domain Smoke Test and the transient Supabase gateway retry fix were accepted on 2026-09-14. Preserve the Smoke script as a release gate; do not repeat this item unless production regresses.
2. **Paused for correctness and release safety — improve Knowledge recall accuracy.** First finish the 2026-09-20 time-zone/concurrent-task regression checks and deploy the matching migration before resuming the 120 six-week core questions. Split long reference sentences into atomic key points; add abbreviations, synonyms, and spoken equivalents; and build a regression set. Keep the two coverage scores separately auditable; AI semantic coverage is currently a TypeScript mastery input, not a direct database write.
3. **Implemented in source, awaiting acceptance — compare AI Knowledge recall history.** `lib/knowledge/recall-history.ts` and `components/knowledge/recall-history-panel.tsx` reconstruct saved AI analysis after reload, compare consecutive Recall omissions and generate one focused next-review hint; no new DB fields or migration. If one side lacks AI, use deterministic points for both. Use the point texts stored on each Attempt to avoid mismatching revised source indexes. Regression tests for local storage, repeated cloud snapshot and UI are written but have not run; do not mark production complete until full gates and real account refresh/relogin pass. Another agent owns the 904-answer corpus; do not edit their files while completing this feature.
4. **Shorten the daily workflow.** Add a direct next-task action, one-click continuation from Dashboard, an estimated daily workload, and a concise end-of-day summary covering completed work, omissions, backlog, and upcoming reviews.
5. **Calibrate the review strategy with evidence.** Keep the current deterministic mastery-bucket intervals while collecting real review outcomes. Record enough evidence to evaluate scheduled interval, actual delay, recall/result quality, hints or independence, and lapses. Do not adopt FSRS, SM-2, or a custom forgetting-curve model merely by name; change the scheduler only after replaying real history shows a measurable retention or workload improvement. Algorithm and Knowledge may require different calibration, and past Attempts must remain immutable.
6. **Complete AI safeguards.** Preserve the existing timeout, structured validation, server-only secrets, and duplicate-click protection. Add per-user daily request limits, explicit input-size limits, user-visible graceful fallback, and log redaction. API keys must never use a `NEXT_PUBLIC_` prefix.
7. **Harden data safety and operations.** Add user-visible JSON export before any destructive migration, document restore and rollback steps, surface actionable production errors without exposing secrets, and verify database indexes and request logs using real usage evidence rather than speculative optimization.

Current execution order: first validate pause/resume implementation (Node quality gate, staging migration `202609200002_plan_pause.sql`, real account, two tabs and DST), then verify the second-round incremental response regression and earlier migration/deployment status → split initial Snapshot into paginated history/summary → add AI per-user quotas → unify input schema validation. Keep existing Knowledge matching/history improvements and daily workflow work on the roadmap; do not add more modules before stability checks. The V1 is a personal tracker, not a tamper-proof scoring platform: client-submitted AI analysis can affect mastery, and RLS permits users to edit their own records. Require a separate trusted-scoring architecture decision before offering certification, rankings or anti-cheat claims.
